# Django Settings
DJANGO_SETTINGS_MODULE=pharmacy_backend.settings

# Debug Mode
DEBUG=True

# Secret Key (CHANGE THIS in production!)
SECRET_KEY=change-me-for-local-dev

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database URL (PostgreSQL)
DATABASE_URL=postgres://user:password@localhost:5432/localdbname

# Frontend URL
FRONTEND_URL=https://pharmafrontend.z29.web.core.windows.net

# CORS Allowed Origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://pharmafrontend.z29.web.core.windows.net

# CSRF Trusted Origins (comma-separated)
CSRF_TRUSTED_ORIGINS=https://pharmafrontend.z29.web.core.windows.net
